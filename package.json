{
  "name": "root",
  "private": true,
  "workspaces": [],
  "devDependencies": {
    "commitizen": "^4.2.5",
    "conventional-changelog-conventionalcommits": "^5.0.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^8.0.1",
    "lerna": "^5.5.1",
    "nx": "^14.7.6"
  },
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build --no-private",
    "clean": "lerna clean",
    "storybook:react": "pnpm run --filter=@dynamic-framework/ui-react storybook",
    "build:storybook:react": "pnpm run --filter=@dynamic-framework/ui-react build-storybook",
    "eslint": "npx lerna run eslint --no-private",
    "stylelint": "npx lerna run stylelint --no-private",
    "test": "npx lerna run test --no-private",
    "publish": "npx lerna publish --no-private --loglevel error"
  },
  "engines": {
    "node": ">=16.16",
    "pnpm": ">=7.29"
  },
  "pnpm": {
    "patchedDependencies": {
      "@stencil/react-output-target@0.5.3": "patches/@stencil__react-output-target@0.5.3.patch"
    }
  }
}
