import {
  Meta,
  Story,
  Source,
} from '@storybook/blocks';

import * as useFormatCurrencyStories from './useFormatCurrency.stories';

<Meta of={useFormatCurrencyStories} />

## Example

<Story of={useFormatCurrencyStories.Default} />
<br />

### DContextProvider Setup - render of `Bootstrap.tsx`
<Source
  code={`
<DContextProvider>
  <App />
</DContextProvider>
  `}
/>

### Hook usage - call
<Source
  code={`
const { values } = useFormatCurrency(100, 234.12, -233);
  `}
/>

### Hook usage - render of `App.tsx`
<Source
  language="tsx"
  code={`
<>
  {values.map((value) => (<pre key={value}>{value}</pre>))}
</>
  `}
/>

