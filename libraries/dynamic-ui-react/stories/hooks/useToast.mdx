import {
  Meta,
  Source,
  Unstyled,
} from '@storybook/blocks';

import {
  ExampleSimpleToastRoot,
  ExampleFullToastRoot,
  ExampleCustomToastRoot,
} from "./useToastExamples";

import DAlert from '../../src/components/DAlert/DAlert';

<Meta title="Design System/Patterns/Toast" />

# Toast

![Shield Badge](https://img.shields.io/badge/Abstraction%20Component-4848b7)

Toasts use `DToastContainer` and the `useDToast` hook, which are abstractions to configure react-hot-toast in conjunction with
a `DToast` to generate toasts. For more detailed information, refer to the official [React Hot Toast Documentation](https://react-hot-toast.com/docs).
<br />

<Unstyled>
  <DAlert theme="warning">
    <span>
      Toast appears in all containers because of the way react-hot-toast is programmed, however,
      if you want to see the correct toast make it show up from the correct container.
    </span>
  </DAlert>
</Unstyled>

## Usage DToastContainer

To get started, include the **`<DToastContainer />`** component once in your app, typically near the root of your component tree.
This ensures that all toasts are rendered correctly. Here you can customize general styles for all toasts and configure a default
placement for displaying toasts. You can override this position in any single toast if needed.

## Usage useDToast

The `useDToast` hook provides a simple and flexible way to create toast notifications in your application. By **default**, it uses the
`DToast` component to display the toast message. However, you can also render a custom component if needed.

Below you will see examples of the use of toast in a simple way, with a custom component and with Material Icons.

## Basic toast no theme

<Canvas>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleSimpleToastRoot />
  </div>
</Canvas>

<Source
  code={
  `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            { title: 'Example' },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>

## Basic toast secondary theme

<Unstyled>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleSimpleToastRoot type='secondary'/>
  </div>
</Unstyled>

<Source
  code={
  `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            {
              title: 'Example',
              theme: 'secondary',
            },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>

## Basic toast secondary soft

<Canvas>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleSimpleToastRoot type='soft' />
  </div>
</Canvas>

<Source
  code={
  `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            {
              title: 'Example',
              theme: 'secondary',
              soft: true,
            },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>


## Full toast no theme

<Canvas>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleFullToastRoot type='base' />
  </div>
</Canvas>

<Source
  code={
    `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            {
              title: 'Example',
              description: 'This is a description',
              timestamp: 'just now',
              icon: 'check',
            },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>

## Full toast secondary theme

<Unstyled>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleFullToastRoot type='secondary' />
  </div>
</Unstyled>

<Source
  code={
    `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            {
              title: 'Example',
              description: 'This is a description',
              timestamp: 'just now',
              theme: 'secondary',
              icon: 'check',
            },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>

## Full toast secondary soft

<Canvas>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleFullToastRoot type='soft' />
  </div>
</Canvas>

<Source
  code={
    `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            {
              title: 'Example',
              description: 'This is a description',
              timestamp: 'just now',
              theme: 'secondary',
              soft: true,
              icon: 'check',
            },
            { duration: 40000 },
          )
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
}
`
  }
  language="tsx"
  dark
/>


## Custom example

<Canvas>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '200px' }}>
    <ExampleCustomToastRoot />
  </div>
</Canvas>

### `App.tsx`

<Source
  code={
    `
import {
  DContextProvider,
  DToastContainer,
  useDToast,
} from '@dynamic-framework/ui-react';

import CustomToastExample from './CustomToastExample';

export function App() {
  const { toast } = useDToast();

  return (
    <DContextProvider>
      <DButton
        text="Show Toast"
        onClick={() => (
          toast(
            CustomToastExample,
          {
            duration: 3000,
          })
        )}
      />
      <DToastContainer
        position="top-right"
      />
    </DContextProvider>
  );
    }
`
  }
  language="tsx"
  dark
/>

### `CustomToastExample.tsx`

<Source
  code={
    `
import {
  DButton,
} from '@dynamic-framework/ui-react';
import { toast as reactToast, Toast } from 'react-hot-toast';

export function CustomToastExample({ id, visible }: Toast) {
  if (!visible) {
    return null;
  }
  return (
    <div className="bg-surface-secondary rounded-2 p-4 text-center">
      <p className="fw-bold mt-0">Toast!</p>
      <DButton
        size="sm"
        variant="outline"
        theme="secondary"
        text="Close toast"
        onClick={() => reactToast.dismiss(id)}
      />
    </div>
  );
}
`
  }
  language="tsx"
  dark
/>

<Unstyled>
  <div className="d-flex flex-column justify-content-end align-items-end" style={{ height: '300px' }}>
    <ExampleCustomToastRoot />
  </div>
</Unstyled>
