import {
  Meta,
  ArgTypes,
  Markdown,
  Stories,
  Story,
  Source,
} from '@storybook/blocks';
import { PREFIX_BS } from '../../src/components/config';

import * as DToastStories from './DToast.stories';
import * as useToastStories from '../hooks/useToast.stories';

# Toast

Alert Component.

To understand in more detail the aspects covered by this component, review the following documentation:

+ [Bootstrap Alerts](https://getbootstrap.com/docs/5.3/components/alerts/)
+ [React Toastify](https://fkhadra.github.io/react-toastify/introduction/)

## CSS Variables
<Markdown>
  {`

  | Variable                                      | Type             | Description              |
  |-----------------------------------------------|------------------|--------------------------|
  | \`--${PREFIX_BS}alert-gap\`                   | css length unit  | Content separation       |
  | \`--${PREFIX_BS}alert-box-shadow\`            | css box shadow   | Toast box shadow         |
  | \`--${PREFIX_BS}alert-icon-color\`            | css color unit   | Toast icon color         |
  | \`--${PREFIX_BS}alert-separator-opacity\`     | css length unit  | Toast separator opacity  |
  | \`--${PREFIX_BS}alert-close-icon-size\`       | css length unit  | Toast close icon size    |

  `}
</Markdown>

## Args
<Meta of={DToastStories} />

<ArgTypes of={DToastStories} />

## Example

<Story of={useToastStories.useToast} />
<br />

### Toast Usage
<Source
  code={`
<>
  <DButton
    text="Show Toast"
    onClick={() => toast(message, { type: 'info', showClose: false })}
  />
  <DToastContainer />
</>
  `}
/>

<Stories />


