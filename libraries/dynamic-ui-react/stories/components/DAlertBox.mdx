import {
  Meta,
  ArgTypes,
  Markdown,
  Stories,
} from '@storybook/blocks';
import { PREFIX_BS } from '../../src/components/config';

import * as DAlertBoxStories from './DAlertBox.stories';

# Alert Box

## CSS Variables
<Markdown>
  {`

  | Variable                                      | Type             | Description              |
  |-----------------------------------------------|------------------|--------------------------|
  | \`--${PREFIX_BS}alert-gap\`                   | css length unit  | Content separation       |
  | \`--${PREFIX_BS}alert-box-shadow\`            | css box shadow   | Alert box shadow         |
  | \`--${PREFIX_BS}alert-icon-color\`            | css color unit   | Alert icon color         |
  | \`--${PREFIX_BS}alert-separator-opacity\`     | css length unit  | Alert separator opacity  |
  | \`--${PREFIX_BS}alert-close-icon-size\`       | css length unit  | Alert close icon size    |

  `}
</Markdown>

## Args
<Meta of={DAlertBoxStories} />

<ArgTypes of={DAlertBoxStories} />

<Stories />


