import {
  Meta,
  ArgTypes,
  Stories,
  Markdown,
} from '@storybook/blocks';
import { PREFIX_BS } from '@dynamic-framework/ui';

import * as MStepper from './MStepper.stories';

# Stepper

## CSS Variables
<Markdown>
  {`

  | Variable                                     | Type             | Description                   |
  |----------------------------------------------|------------------|-------------------------------|
  | \`--${PREFIX_BS}m-icon-container-size\`      | css length unit  | Icon container size           |
  | \`--${PREFIX_BS}m-step-label-height\`        | css length unit  | Minimum label height          |
  | \`--${PREFIX_BS}m-step-icon-size\`           | css length unit  | Step icon size                |

  `}
</Markdown>

## Args
<Meta of={MStepper} />

<ArgTypes of={MStepper} />

<Stories />

