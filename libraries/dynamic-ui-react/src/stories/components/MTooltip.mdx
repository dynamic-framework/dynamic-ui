import {
  Meta,
  ArgTypes,
  Stories,
  Markdown,
} from '@storybook/blocks';
import { PREFIX_BS } from '@modyo-dynamic/modyo-design-system';

import * as MTooltipStories from './MTooltip.stories';

# Tooltip

Tooltip Component.

To understand in more detail the aspects covered by this component, review the following documentation:

+ [Floating UI](https://floating-ui.com/docs/react)

## CSS Variables
<Markdown>
  {`

  | Variable                                     | Type             | Description                   |
  |----------------------------------------------|------------------|-------------------------------|
  | \`--${PREFIX_BS}m-tooltip-bg\`               | css color unit   | Background and arrow color    |
  | \`--${PREFIX_BS}m-tooltip-border-radius\`    | css length unit  | Border radius                 |
  | \`--${PREFIX_BS}m-tooltip-color\`            | css color unit   | Text color                    |
  | \`--${PREFIX_BS}m-tooltip-font-size\`        | css length unit  | Text font size                |
  | \`--${PREFIX_BS}m-tooltip-padding\`          | css length unit  | Padding                       |

  `}
</Markdown>

## Args
<Meta of={MTooltipStories} />

<ArgTypes of={MTooltipStories} />

<Stories />

