import {
  Meta,
  ArgTypes,
  Markdown,
  Stories,
  Story,
  Source,
} from '@storybook/blocks';
import { PREFIX_BS } from '../../components/config';

import * as DAlertStories from './DAlert.stories';
import * as useToastStories from '../../stories/hooks/useToast.stories';

# Alert

## CSS Variables
<Markdown>
  {`

  | Variable                                      | Type             | Description              |
  |-----------------------------------------------|------------------|--------------------------|
  | \`--${PREFIX_BS}alert-gap\`                   | css length unit  | Content separation       |
  | \`--${PREFIX_BS}alert-box-shadow\`            | css box shadow   | Alert box shadow         |
  | \`--${PREFIX_BS}alert-icon-color\`            | css color unit   | Alert icon color         |
  | \`--${PREFIX_BS}alert-separator-opacity\`     | css length unit  | Alert separator opacity  |

  `}
</Markdown>

## Args
<Meta of={DAlertStories} />

<ArgTypes of={DAlertStories} />

## Example

<Story of={useToastStories.useToast} />
<br />

### Toast Usage
<Source
  code={`
<>
  <DButton
    text="Show Toast"
    onClick={() => toast(message, { type: 'info', showClose: false })}
  />
  <DToastContainer />
</>
  `}
/>

<Stories />


