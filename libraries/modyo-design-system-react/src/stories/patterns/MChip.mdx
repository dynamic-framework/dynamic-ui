import {
  Meta,
  ArgTypes,
  Stories,
  Markdown,
} from '@storybook/blocks';
import { PREFIX_BS } from '@modyo-dynamic/modyo-design-system';

import * as MChipStories from './MChip.stories';

# Chip

// TODO

## CSS Variables
<Markdown>
  {`

  | Variable                                  | Type            | Description                        |
  |-------------------------------------------|-----------------|------------------------------------|
  | \`--${PREFIX_BS}m-chip-bg\`               | css color unit  | Background color                   |
  | \`--${PREFIX_BS}m-chip-gap\`              | css length unit | Separation between chip elements   |
  | \`--${PREFIX_BS}m-chip-color\`            | css color unit  | Text color                         |
  | \`--${PREFIX_BS}m-chip-border-radius\`    | css length unit | Border radius                      |
  | \`--${PREFIX_BS}m-chip-padding-x\`        | css length unit | Padding horizontal                 |
  | \`--${PREFIX_BS}m-chip-padding-y\`        | css length unit | Padding vertical                   |
  | \`--${PREFIX_BS}m-chip-font-size\`        | css length unit | Font size                          |
  | \`--${PREFIX_BS}m-chip-font-weight\`      | css font weight | Font weight                        |
  | \`--${PREFIX_BS}m-chip-line-height\`      | css length unit | Line height                        |

  `}
</Markdown>

## Args
<Meta of={MChipStories} />

<ArgTypes of={MChipStories} />

<Stories />


