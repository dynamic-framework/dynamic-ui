import {
  Meta,
  ArgTypes,
  Markdown,
  IconGallery,
  IconItem,
  Stories,
} from '@storybook/blocks';
import { PREFIX_BS } from '@modyo-dynamic/modyo-design-system';

import { ICONS } from '../constants';

import * as MIconStories from './MIcon.stories';
import { MIcon } from '../../components';

# Icon

Wrapper Component around Bootstrap Icons.

Free, high quality, open source icon library with over 1,800 icons. Include them anyway you like—SVGs, SVG sprite, or web fonts. Use them with or without Bootstrap in any project.

Source site: [Bootstrap Icons](https://icons.getbootstrap.com/)

## CSS Variables
<Markdown>
  {`

  | Variable                                 | Type            | Description                           |
  |------------------------------------------|-----------------|---------------------------------------|
  | \`--${PREFIX_BS}m-icon-color\`            | css color unit  | The icon color                        |
  | \`--${PREFIX_BS}m-icon-bg-color\`         | css color unit  | The icon background color             |
  | \`--${PREFIX_BS}m-icon-size\`             | css length unit | The size of icon                      |
  | \`--${PREFIX_BS}m-icon-padding\`          | css length unit | The padding to make icon circle       |
  | \`--${PREFIX_BS}m-icon-loading-duration\` | number          | The duration of the loading animation |

  > The colors can ref another css var like a \`var(--${PREFIX_BS}primary)\`

  `}
</Markdown>

## Args
<Meta of={MIconStories} />

<ArgTypes of={MIconStories} />

<Stories />

# Iconography

<IconGallery>
  {ICONS.map((name) => (
    <IconItem key={name} name={name}>
      <MIcon icon={name} />
    </IconItem>
  ))}
</IconGallery>

