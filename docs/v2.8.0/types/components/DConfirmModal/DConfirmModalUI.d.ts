/// <reference types="react" />
import type { ConfirmModalEntry } from './confirmModalStore';
type Props = {
    entry: ConfirmModalEntry;
};
export default function DConfirmModalUI({ entry }: Props): import("react").JSX.Element;
export {};
